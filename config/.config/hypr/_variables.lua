-- =============================================================================
-- _variables.lua — personal overrides
-- =============================================================================

local variables = __require("variables")

variables.webapp = 'google-chrome --profile-directory="Default" --app='

return variables
